{
  "version": 2,
  "buildCommand": "npm install --legacy-peer-deps && npm install tailwindcss postcss autoprefixer --no-save && NODE_ENV=production CI=false npm run build",
  "installCommand": "npm install --legacy-peer-deps",
  "framework": "nextjs",
  "ignoreCommand": "echo 'Ignoring failed command' && exit 0"
} 